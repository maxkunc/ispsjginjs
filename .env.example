# Base URL of the OpenSchoolSucks Flask API (see the openschoolsucks repo).
# In dev this can be left unset - vite.config.js proxies /api to
# http://127.0.0.1:5000 automatically. In production, point it at the
# deployed backend, e.g. https://api.example.com/api
VITE_API_URL=/api
